import { CommonRegisterRequest } from './common-register-request';

describe('CommonRegisterRequest', () => {
  it('should create an instance', () => {
    expect(new CommonRegisterRequest()).toBeTruthy();
  });
});
